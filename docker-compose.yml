version: '3.8'

services:
  web:
    build: ./website
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./website/:/usr/src/website/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev